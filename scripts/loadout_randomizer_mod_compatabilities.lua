local mod_compatabilities = {
    ["BetterLoadouts"] = {
        randomizer_profile_widget ={
            offset_x = 0,
            offset_y = 0,
        },
    },
}

local get_loaded_mod_compatibilities = function()
    for mod_key, mod_settings in pairs(mod_compatabilities)
        local mod_match = mod_get(mod_match)

        if mod_match then
        end
    end
end